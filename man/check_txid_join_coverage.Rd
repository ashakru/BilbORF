% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/orf_footprints.R
\name{check_txid_join_coverage}
\alias{check_txid_join_coverage}
\title{Check Transcript ID Join Coverage Against a TxDb}
\usage{
check_txid_join_coverage(calls_df, txdb)
}
\arguments{
\item{calls_df}{A data frame with at least a \code{transcript_id} character
column. Additional columns are ignored.}

\item{txdb}{A TxDb object (e.g. from \code{\link[GenomicFeatures:makeTxDbFromGFF]{GenomicFeatures::makeTxDbFromGFF()}}).}
}
\value{
A named list with two elements:
\describe{
\item{\code{summary}}{A \code{\link[tibble:tibble]{tibble::tibble()}} with columns \code{tier}, \code{n},
\code{pct}, \code{n_par}, \code{n_nonstandard_scaffold}.}
\item{\code{unresolvable_ids}}{Character vector of transcript_ids that could
not be resolved at any tier.}
}
}
\description{
Run BEFORE any reconstruction step. Reports how many caller-supplied
\code{transcript_id}s resolve against the TxDb, with explicit handling of
GENCODE version suffixes (\code{ENST00000123456.7} → \code{ENST00000123456}).
}
\details{
Match tiers (applied in order, mutually exclusive):
\enumerate{
\item \strong{exact} — id found verbatim in TxDb.
\item \strong{strip} — id found after removing \verb{\\\\.\\\\d+$} suffix.
\item \strong{unresolvable} — not found at either tier.
}

PAR-locus IDs (\verb{_PAR_Y} suffix) and non-standard scaffold IDs
(seqnames not in the standard 1–22, X, Y, MT / chr1–22, chrX, chrY, chrM
set) are flagged separately within each tier.

Nothing is dropped. The function is purely diagnostic.
}
