% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/orf_footprints.R
\name{filter_chains_by_junctions}
\alias{filter_chains_by_junctions}
\title{Filter Chain-Level Footprints by BAM Junction Evidence}
\usage{
filter_chains_by_junctions(
  footprints,
  bam_files,
  min_reads = 2L,
  require_same_sample = FALSE
)
}
\arguments{
\item{footprints}{A named \code{\link[GenomicRanges:GRangesList-class]{GenomicRanges::GRangesList()}} as returned by
\code{\link[=parse_orf_footprints]{parse_orf_footprints()}}.}

\item{bam_files}{A \strong{named} character vector of BAM file paths. Names must
match run identifiers in \code{mcols(footprints)$called_by_run} when
\code{require_same_sample = TRUE}.}

\item{min_reads}{Integer. Minimum number of junction-spanning reads required
per junction. Default \code{2L}.}

\item{require_same_sample}{Logical. If \code{TRUE}, a chain is assessed only
against BAMs from the runs that called it (via \code{called_by_run} in
\code{mcols}). Requires \code{parse_orf_footprints()} to have been run with a
\code{calls_df} that had a \code{run} column. If no matching BAMs are found for a
chain, it is kept (cannot be assessed). Default \code{FALSE} (all BAMs pooled).}
}
\value{
A filtered \code{\link[GenomicRanges:GRangesList-class]{GenomicRanges::GRangesList()}} with the same structure as
the input. A \code{junction_reads} integer list-column is added to \code{mcols},
recording the per-junction read counts used for the decision
(\code{NA} for single-exon or failed-reconstruction chains).
}
\description{
Retains only chains whose internal intron boundaries are supported by
junction-spanning reads in one or more BAM files. Single-exon chains and
chains with failed reconstruction pass unconditionally (they have no
junctions to check).
}
\section{Algorithm}{

\enumerate{
\item Extract all internal intron GRanges from multi-exon chains.
\item Read junction-spanning reads from each BAM restricted to those regions
via \code{\link[GenomicAlignments:junctions-methods]{GenomicAlignments::summarizeJunctions()}}.
\item Match observed junctions to chain junctions by exact position
(\code{\link[GenomicRanges:findOverlaps-methods]{GenomicRanges::findOverlaps()}} with \code{type = "equal"}).
\item A chain passes if \strong{every} one of its junctions has ≥ \code{min_reads}
support in the relevant BAMs.
}
}
