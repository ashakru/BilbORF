% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/orf_footprints.R
\name{make_chain_id}
\alias{make_chain_id}
\title{Compute a Deterministic Chain ID for an ORF Footprint}
\usage{
make_chain_id(footprint)
}
\arguments{
\item{footprint}{A \code{\link[GenomicRanges:GRanges-class]{GenomicRanges::GRanges()}} as returned by
\code{\link[=reconstruct_chain]{reconstruct_chain()}}: clipped exon blocks in any order (the function
sorts internally). Must all share the same strand.}
}
\value{
A length-1 character string: a SHA-1 hex digest prefixed with
\code{"chain_"}, e.g. \code{"chain_3a7f9b..."}.
}
\description{
A \strong{chain_id} identifies the unique spliced path of an ORF — its codon
walk. Two calls with different \code{transcript_id}s that produce identical
exonic paths from start to stop will receive the \strong{same} chain_id and
will be collapsed to a single footprint.
}
\details{
\subsection{What is hashed}{

The canonical path representation is a character string of the form:

\if{html}{\out{<div class="sourceCode">}}\preformatted{strand:orf_left:orf_right:j1-j2:j3-j4:...
}\if{html}{\out{</div>}}

where:
\itemize{
\item \code{strand}    — \code{+} or \code{-}.
\item \code{orf_left}  — numerically smaller genomic boundary (= start on \code{+},
stop on \code{-}).
\item \code{orf_right} — numerically larger  genomic boundary.
\item \code{j1-j2}, \code{j3-j4}, … — \strong{internal intron boundaries}, sorted
numerically ascending, each formatted as \code{intron_start-intron_end}.
An intron boundary is derived from consecutive exon blocks: if exon k
ends at E and exon k+1 starts at S (both in ascending genomic order),
the intron is \code{(E+1)-(S-1)}. Single-exon ORFs have no junction tokens.
}
}

\subsection{What is NOT hashed}{
\itemize{
\item \code{transcript_id} — by design. Cosmetic label differences must not
create separate chains.
\item UTR exons or exons entirely outside \link{orf_left, orf_right}.
\item Anything downstream of the stop codon.
}
}

\subsection{Identity guarantee}{

Including both \code{orf_left}/\code{orf_right} (i.e. both start and stop) in the
hash ensures that transcripts sharing internal junctions but differing in
start codon position produce \strong{different} chain_ids, reflecting genuinely
different codon walks at the N-terminus.
}
}
