% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/normalise_and_reannotate_orfs.R
\name{normalise_and_reannotate_orfs}
\alias{normalise_and_reannotate_orfs}
\title{Normalise ORF stops and re-annotate isoforms in one pass}
\usage{
normalise_and_reannotate_orfs(
  orfs,
  transcripts,
  cds_by_tx,
  genome,
  id_col = NULL,
  type_priority = .BILBORF_REFERENCE_ORF_TYPE_PRIORITY,
  stop_codon_convention = c("excluded", "included", "auto"),
  stop_codons = c("TAA", "TAG", "TGA"),
  allow_terminal_extrapolation = TRUE,
  max_terminal_overhang = 30L,
  unmatched_type = NA_character_
)
}
\arguments{
\item{orfs}{A \code{GRanges} of unique ORF spans or a \code{GRangesList} of unique ORF
chains. Coordinates are 1-based and inclusive.}

\item{transcripts}{A named \code{GRangesList} of transcript exons.}

\item{cds_by_tx}{A named \code{GRangesList} of CDS exons.}

\item{genome}{A genome accepted by
\code{\link[GenomicFeatures:extractTranscriptSeqs]{GenomicFeatures::extractTranscriptSeqs()}}, such as a \code{BSgenome}.}

\item{id_col}{Optional ORF-ID metadata column; otherwise \code{names(orfs)} are
used.}

\item{type_priority}{Priority used to select among valid transcript
interpretations.}

\item{stop_codon_convention}{Whether the supplied 3' boundary includes or
excludes the stop codon. \code{"auto"} accepts a convention only when exactly
one of the endpoint triplet and following triplet is a canonical stop.}

\item{stop_codons}{Accepted stop triplets in transcript orientation.}

\item{allow_terminal_extrapolation, max_terminal_overhang}{Passed to
\code{\link[=reannotate_orf_type]{reannotate_orf_type()}}. Stop-codon normalisation itself requires the
canonical endpoint to exist on a compatible transcript; a longer isoform
is therefore preferred over extrapolating an absent terminal exon.}

\item{unmatched_type}{Passed to \code{\link[=reannotate_orf_type]{reannotate_orf_type()}}.}
}
\value{
A list with \code{orfs}, \code{pairs}, and \code{canonical_chains}. \code{orfs} contains
one row per input ORF. \code{pairs} retains every candidate ORF-transcript
interpretation and its stop-normalisation audit. \code{canonical_chains} is a
stop-inclusive \code{GRangesList} for successfully normalised ORFs, named by
ORF ID.
}
\description{
Reuse the ORF-transcript mappings produced by \code{\link[=reannotate_orf_type]{reannotate_orf_type()}} to
validate an included or omitted stop codon, select a reference isoform, and
return the corresponding canonical stop-inclusive exon chain. This avoids
rebuilding exon intersections separately for stop normalisation and type
annotation.
}
