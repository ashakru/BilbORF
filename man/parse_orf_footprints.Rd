% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/orf_footprints.R
\name{parse_orf_footprints}
\alias{parse_orf_footprints}
\title{Parse ORF Calls into a Chain-Level GRangesList of Footprints}
\usage{
parse_orf_footprints(calls_df, txdb)
}
\arguments{
\item{calls_df}{A data frame with columns:
\describe{
\item{\code{start}}{Integer. Genomic start (A of start codon; plus strand:
smaller coord; minus strand: larger coord).}
\item{\code{stop}}{Integer. Genomic stop (last nt of stop codon).}
\item{\code{strand}}{Character \code{"+"} or \code{"-"}.}
\item{\code{transcript_id}}{Character. GENCODE transcript ID (with or without
version suffix). Will be matched against \code{exons_by_tx} names; version
stripping is attempted automatically.}
\item{\code{caller}}{Character. Name of the calling tool
(e.g. \code{"RiboTIE"}, \code{"RiboCode"}, \code{"ORFquant"}).}
\item{\code{run}}{Character, \strong{optional}. Sample/run identifier (e.g.
\code{"RT_HEK293_3"}). When present, aggregated into a \code{called_by_run}
list-column used by \code{\link[=filter_chains_by_junctions]{filter_chains_by_junctions()}}. Rows with
\code{NA} run (e.g. from all-transcript enumeration) are silently excluded
from \code{called_by_run}.}
}}

\item{txdb}{A TxDb object.}
}
\value{
A named \code{\link[GenomicRanges:GRangesList-class]{GenomicRanges::GRangesList()}} where names are \code{chain_id}
values, and \code{mcols} contains:
\describe{
\item{\code{chain_id}}{Character.}
\item{\code{translon_id}}{Character.}
\item{\code{start_id}}{Character.}
\item{\code{n_exons}}{Integer. Number of exon blocks in the chain.}
\item{\code{single_exon}}{Logical.}
\item{\code{frame}}{Integer 0/1/2.}
\item{\code{compatible_transcripts}}{\code{\link[IRanges:AtomicList-class]{IRanges::CharacterList()}}.}
\item{\code{called_by}}{\code{\link[S4Vectors:SimpleList-class]{S4Vectors::SimpleList()}} of named character vectors,
caller → transcript_ids.}
\item{\code{called_by_run}}{\code{\link[IRanges:AtomicList-class]{IRanges::CharacterList()}}. Run names that called
this chain. Only present when \code{calls_df} has a \code{run} column.}
\item{\code{n_calls}}{Integer. Number of raw calls collapsed here.}
\item{\code{chain_reconstructed}}{Logical. FALSE if reconstruction failed.}
}
}
\description{
Top-level constructor. Accepts a data frame of ORF calls (potentially from
multiple callers), reconstructs the spliced path for each call, groups by
\code{chain_id}, and returns one \code{\link[GenomicRanges:GRangesList-class]{GenomicRanges::GRangesList()}} element per
distinct chain.
}
\details{
Collapse semantics:
\itemize{
\item Two calls producing the same spliced ORF path → \strong{one} footprint.
\item \code{compatible_transcripts} in \code{mcols}: all transcript IDs whose paths
collapsed here (set-valued; stored as a \code{CharacterList}).
\item \code{called_by} in \code{mcols}: named list mapping caller → transcript_id it
used for this chain.
\item Calls whose reconstruction failed are retained with
\code{chain_reconstructed = FALSE} and a synthetic GRanges placeholder
(the genomic interval start–stop with no exon structure).
}
}
