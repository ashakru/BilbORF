% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reannotate_orf_type.R
\name{reannotate_orf_type}
\alias{reannotate_orf_type}
\title{Re-annotate ORF type from transcript and CDS geometry}
\usage{
reannotate_orf_type(
  orfs,
  transcripts,
  cds_by_tx,
  id_col = NULL,
  type_priority = .BILBORF_REFERENCE_ORF_TYPE_PRIORITY
)
}
\arguments{
\item{orfs}{A \code{GRanges} with one genomic bounding range per ORF, or a
\code{GRangesList} with one exon-resolved range chain per ORF. Each ORF must use
one seqname and an explicit \code{+} or \code{-} strand. Coordinates must use the
same inclusive boundary convention as \code{cds_by_tx}.}

\item{transcripts}{A \code{GRangesList} of transcript exons with unique transcript
IDs in \code{names(transcripts)}.}

\item{cds_by_tx}{A \code{GRangesList} of CDS exons named in the same transcript-ID
space as \code{transcripts}. Non-coding transcripts may be absent.}

\item{id_col}{Optional metadata-column name in \code{mcols(orfs)} containing
unique ORF IDs. When \code{NULL}, \code{names(orfs)} are used; sequential IDs are
generated only when names are absent.}

\item{type_priority}{Character vector containing every value returned by
\code{\link[=orf_type_levels]{orf_type_levels()}} exactly once. It determines which interpretation wins
when an ORF has different types on different transcript isoforms.}
}
\value{
A list with two tibbles:
\describe{
\item{\code{orfs}}{One row per input ORF, including the selected
\code{reference_orf_type}, \code{reference_orf_class}, selected transcript,
frame flags, and ambiguity summaries.}
\item{\code{pairs}}{One row per candidate ORF-transcript pair, retaining all
projected coordinates, classifications, and failure statuses for audit.}
}
}
\description{
Classify genomic ORFs relative to every compatible reference transcript and
its annotated CDS. Genomic boundaries are mapped to spliced transcript
coordinates with ORFik, so exon junctions and negative-strand transcripts
are handled in transcript orientation.
}
\details{
A transcript is compatible when both ORF boundaries map to its exons. For
\code{GRangesList} input, the complete ORF exon chain must additionally equal the
transcript's exonic coverage between those boundaries. Thus transcripts with
incompatible internal splice junctions are excluded. A single-range
\code{GRanges} does not contain enough information for this splice-chain check and
retains boundary-only compatibility. The following mutually exclusive rules
are evaluated in transcript coordinates:
\itemize{
\item exact CDS boundaries: \verb{annotated CDS};
\item same CDS 3' boundary and an in-frame upstream/downstream 5' boundary:
\verb{N-terminal extension} or \verb{N-terminal truncation};
\item same CDS 5' boundary and an in-frame downstream/upstream 3' boundary:
\verb{C-terminal extension} or \verb{C-terminal truncation};
\item in-frame extension beyond both CDS boundaries: \verb{NC-terminal extension};
\item wholly upstream/downstream: \code{uORF}/\code{dORF};
\item overlapping the CDS from upstream/downstream: \code{uoORF}/\code{doORF};
\item contained within the CDS after the exact/truncation rules: \code{intORF};
\item a compatible transcript without a CDS, or no compatible transcript:
\code{varRNA-ORF}.
}

Exact CDS and in-frame canonical variants take precedence over a
non-canonical interpretation on another isoform by default. This function
uses geometry only; it does not inspect genomic sequence for start codons,
stop codons, or internal stops.
}
\examples{
library(GenomicRanges)
tx <- GRangesList(
  tx1 = GRanges("chr1", IRanges(100, 499), strand = "+")
)
cds <- GRangesList(
  tx1 = GRanges("chr1", IRanges(200, 399), strand = "+")
)
orfs <- GRanges("chr1", IRanges(c(200, 197), c(399, 399)), strand = "+")
names(orfs) <- c("exact", "n_extension")

annotation <- reannotate_orf_type(orfs, tx, cds)
annotation$orfs[, c("orf_id", "reference_orf_type")]

}
