% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/orf_footprints.R
\name{reconstruct_chain}
\alias{reconstruct_chain}
\title{Reconstruct Spliced ORF Path from a Transcript}
\usage{
reconstruct_chain(start, stop, strand, transcript_id, exons_by_tx)
}
\arguments{
\item{start}{Genomic coordinate of the first nt of the start codon (A of
ATG). On minus strand this is numerically \strong{greater} than \code{stop}.}

\item{stop}{Genomic coordinate of the last nt of the stop codon. On minus
strand this is numerically \strong{less} than \code{start}.}

\item{strand}{Character \code{"+"} or \code{"-"}.}

\item{transcript_id}{Transcript name as it appears in \code{exons_by_tx} (after
any version-stripping has been applied upstream).}

\item{exons_by_tx}{A named \code{\link[GenomicRanges:GRangesList-class]{GenomicRanges::GRangesList()}} produced by
\code{GenomicFeatures::exonsBy(txdb, by = "tx", use.names = TRUE)}. Must be
precomputed once and reused across calls.}
}
\value{
A \code{\link[GenomicRanges:GRanges-class]{GenomicRanges::GRanges()}} of clipped exon blocks in 5′→3′
coding order, with a \code{exon_rank} integer metadata column (1 = first
coding exon). Returns \code{NULL} (with a warning) if reconstruction fails.
}
\description{
Given genomic start, stop, strand, and a transcript ID, clips the
transcript's exons to the ORF span and returns only the exonic segments
between (and including) the start and stop positions.
}
\details{
The returned GRanges is ALWAYS in 5′→3′ coding order:
\itemize{
\item plus  strand: ascending genomic coordinate.
\item minus strand: descending genomic coordinate (first element has the
largest genomic position, i.e. the A of the start codon).
}
}
